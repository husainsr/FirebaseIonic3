```bash
   1.)npm install
```
```bash
   2.)Add your firebase config to app.module.ts
      also dont forgot to add package name from config.xml to firebase to link your app. 
```
```bash
   I had created 3 fields key,s_name,s_stream.refer item.model.ts to
   change the values as per your requirements
```
```bash
   Now run and enjoy the app.
   
```
